# board component — central pin definitions for the ThinkNode M5.
# See include/board.h for the pin map and its source of truth.
idf_component_register(
    INCLUDE_DIRS "include"
    REQUIRES driver
)
